{
	"name": "@example/starter",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "astro dev",
		"start": "astro dev",
		"build": "astro build",
		"preview": "astro preview",
		"vercel-build": "pnpx prisma generate && prisma migrate deploy && next build"
	},
	"devDependencies": {
		"@astrojs/tailwind": "^0.1.0",
		"@astrojs/vercel": "^0.0.3-beta.1",
		"astro": "^1.0.0-beta.5",
		"prettier": "^2.6.2",
		"prisma": "^3.12.0"
	},
	"dependencies": {
		"@prisma/client": "^3.12.0"
	}
}
